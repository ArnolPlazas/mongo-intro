use("platzi_store")

db.products.updateOne(
  //query
  {_id: 2},
  // set => Sets the value of a field in a document.
  {

    $set: {
      name: 'change name',
      price: 3000,
      tags: ['a', 'b', 'c']
    }
  }
);

